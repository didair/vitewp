---
import type { NotFoundTemplateProps as Props } from 'vite-wp/wordpress/templates';

const { Layout, slug = '' } = Astro.props;
---
<Layout {...Astro.props} title="Not found">
  <article>
    <h1>Not found</h1>
    <p>No WordPress route matched <code>/{slug}</code>.</p>
  </article>
</Layout>
